# Dependencies for `mlflow run .` when NOT using --env-manager local.
# Keep in sync with requirements.txt at the repo root.
python: "3.12"
build_dependencies:
  - pip
dependencies:
  - mlflow>=2.10
  - scikit-learn
  - pandas
